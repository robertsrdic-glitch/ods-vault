# Dnevnik dela — 28. junij 2026

## Povzetek vseh nastavitev

### 1. Claude Code na VPS ✅
- Instaliran Claude Code CLI v2.1.185
- ANTHROPIC_API_KEY nastavljen v `/opt/data/.env`
- `.bashrc` nastavljen (PATH, LD_LIBRARY_PATH, env auto-load)
- tmux 3.5a instaliran lokalno (brez sudo, z libevent + libjemalloc)

### 2. GitHub povezava ✅
- SSH ključ ustvarjen (`~/.ssh/id_ed25519`), dodan na GitHub kot `hermes-vps`
- Git config: `robertsrdic` / `robertsrdic@gmail.com`
- gh CLI v2.95.0 instaliran lokalno
- GitHub username: `robertsrdic-glitch`

### 3. GitHub repota ✅
- **`vibe-projects`** (private) — vibecode projekti
  - Struktura: `claude-code/`, `experiments/`, `learning/`
- **`ods-vault`** (private) — Obsidian vault, kloniran na VPS v `/opt/data/ods-vault`
  - Struktura: Cosmo World, Etsy, Gumroad, Tasks, Vibecode

### 4. Cron jobi ✅ (vsi 0 kreditov — no_agent)

| Job | Urnik | Kaj počne |
|---|---|---|
| `vibe-projects-commit-watcher` | vsakih 30min | Pulla commite, obvesti na Telegram |
| `ods-vault-sync` | vsako uro | Pulla vault, obvesti na Telegram |
| `anthropic-daily-usage-report` | vsak dan 07:00 | Pošlje poročilo o porabi na Gmail + Telegram |

**Quiet hours:** 22:00–06:00 — skripte še vedno pullajo, a ne pošiljajo obvestil

### 5. Gmail povezava ✅
- himalaya CLI v1.2.0 instaliran
- Gmail App Password nastavljen (`robertsrdic@gmail.com`)
- Test: uspešno prebral, poslal in odgovoril na emaile

### 6. Prenovljen predlog projekta za MORS ✅
- Original: `Drone_Training_and_Mission_Simulation_Platform_concept_proposal_SL.docx`
- **Nova slovenska verzija:** modernizirana .docx z naslovnico, executive summary, 12 benefitov za MORS, 8 benefitov za ZenLab, povzetek za netehnične bralce, naslednji koraki
- **Nova angleška verzija:** enaka vsebina v angleščini
- Obe verziji poslani na `robert@zenlab.si`
- Generator skripti: `/opt/data/scripts/generate_proposal.py` (SL) + `generate_proposal_en.py` (EN)

### 7. Firebase ✅
- Firebase CLI v15.22.3 instaliran
- Service Account Key nastavljen (`/opt/data/.config/firebase/vibecode-todo-service-account.json`, dovoljenje 600)
- `GOOGLE_APPLICATION_CREDENTIALS` v `.bashrc`
- firebase-admin v14.1.0 instaliran
- Test: uspešno bral Firestore kolekcijo `notes`
- Projekt: `vibecode-todo` (Spark plan)

### 8. Telegram Desktop ✅
- Robert namestil Telegram Desktop na PC

### 9. Trenutni projekt (zvečer) 📱
- Android aplikacija z Firebase integracijo
- Priprava `.aab`/`.apk` za Google Play Store
- APK signing, `android-keystore`, `icon_512.png`
- Claude spet muti ("Claude 3.5 is currently unavailable")

---

## Pomembne datoteke na VPS

```
/opt/data/
├── .env                          # ANTHROPIC_API_KEY, Telegram, HF_HOME
├── .bashrc                       # PATH, LD_LIBRARY_PATH, env, Firebase
├── .config/
│   ├── himalaya/config.toml      # Gmail config
│   └── firebase/                 # Service account key
├── .local/
│   ├── bin/                      # tmux, gh, firebase
│   ├── lib/                      # libevent, libjemalloc
│   └── firebase-admin/           # Node.js Firebase Admin SDK
├── vibe-projects/                # GitHub repo (clone)
├── ods-vault/                    # Obsidian vault (clone)
├── scripts/
│   ├── generate_proposal.py      # SL predlog generator
│   └── generate_proposal_en.py   # EN predlog generator
└── .hermes/scripts/
    ├── check-vibe-commits.sh     # Cron: vibe-projects watcher
    ├── ods-vault-sync.sh         # Cron: vault sync
    ├── anthropic-usage-report.py # Cron: dnevno poročilo
    └── anthropic-usage-report.sh
```

---

## Workflow za prihodnje delo

1. **Ti delaš** v Claude Code na PC-ju → `git push`
2. **Cron job** samodejno pulla na VPS (30min / 1h)
3. **Jaz vidim kodo** in se učim iz tvojega dela
4. **Ko Claude izgubi sled** — vprašaj mene, jaz imam vse
5. **Poročilo o porabi** vsak dan ob 07:00 na Gmail + Telegram

---

*Ustvaril: Hermes Agent — 28. junij 2026 ob 21:15*
